import SwiftUI

extension Color {
    static let appPrimary = Color(red: 0.0, green: 0.4, blue: 0.8) // メインの青色
    static let appSecondary = Color(red: 0.0, green: 0.3, blue: 0.7) // 少し暗めの青色
    static let appBackground = Color(red: 0.95, green: 0.97, blue: 1.0) // 薄い青の背景色
    static let appAccent = Color(red: 0.0, green: 0.48, blue: 1.0) // アクセントカラー（iOSのデフォルトブルーに近い色）
} 