import SwiftUI

extension Color {
    static let appPrimary = Color(red: 0.0, green: 0.4, blue: 0.8) // Main blue color
    static let appSecondary = Color(red: 0.0, green: 0.3, blue: 0.7) // Slightly darker blue color
    static let appBackground = Color(red: 0.95, green: 0.97, blue: 1.0) // Light blue background color
    static let appAccent = Color(red: 0.0, green: 0.48, blue: 1.0) // Accent color (close to iOS default blue)
}
